<%= form_for [@user, @course, @poll], local: true, html: {:class => "form-horizontal"} do |form| %>
  <% if @poll.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2>
        <%= pluralize(@poll.errors.count, "error") %> prohibited
        this poll from being saved:
      </h2>
      <ul>
        <% @poll.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <table>
    <tr class="field">
      <td><%= form.label :name,  :class => "control-label"  %></td>
      <td><%= form.text_field :name,  :class => "name" %></td>
    </tr>

    <tr class="field">
      <td><%= form.label :description,  :class => "control-label"  %></td>
      <td><%= form.text_area :description, :class => "description" %></td>
    </tr>
  </table>
  <%= form.submit "Confirm" , :class => "btn btn-primary" %>
  <%= link_to cancel_text, cancel_path , :class => "btn btn-danger" %>
<% end %>
